apiVersion: cert-manager.io/v1
kind: ClusterIssuer
metadata:
  name: maitrova-cert
spec:
  acme:
    server: https://acme-v02.api.letsencrypt.org/directory
    email: peraiahgollapalli@gmail.com
    privateKeySecretRef:
      name: maitrova-cert
    solvers:
    - http01:
        ingress:
          class: nginx