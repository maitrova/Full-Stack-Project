# Frontend Configuration
name: frontend
fullnameOverride: frontend

replicaCount: 1

image:
  repository: docker.io/maitrova/maitrova
  tag: frontend
  pullPolicy: Always

# Service Configuration
service:
  type: ClusterIP
  port: 80
  targetPort: 80

# Environment Configuration
env: production

# Application Environment Variables
envVars:
  NODE_ENV: production
  VITE_API_URL: https://narifighter.online/backend

  # Add any frontend env vars here

# Secret Environment Variables (if frontend needs any)
envSecrets:
  # Add any frontend secrets here
  # EXAMPLE: API_KEY: "your-api-key"

# Resource Limits (Optimized for static file serving)
resources:
  limits:
    cpu: "200m"      # Sufficient for serving static files
    memory: 128Mi    # Frontend is lightweight
  requests:
    cpu: "50m"       # Minimal baseline, can burst to limit
    memory: 64Mi     # Typical usage ~40-50Mi

# Health Checks
livenessProbe:
  enabled: false
  httpGet:
    path: /
    port: 80
  initialDelaySeconds: 30
  periodSeconds: 10
  timeoutSeconds: 5
  failureThreshold: 3

readinessProbe:
  enabled: false
  httpGet:
    path: /
    port: 80
  initialDelaySeconds: 10
  periodSeconds: 5
  timeoutSeconds: 3
  failureThreshold: 3

# Autoscaling
autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 2
  targetCPUUtilizationPercentage: 70
  targetMemoryUtilizationPercentage: 80

# Pod Disruption Budget
pdb:
  enabled: true
  minAvailable: 1
